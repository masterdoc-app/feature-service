# Copy to /etc/masterdoc-feature-service/.env on the VPS (CI materializes from secrets).
FEATURE_SERVICE_PORT=8082
ZITADEL_ISSUER=https://auth.example.com
ZITADEL_JWK_SET_URI=https://auth.example.com/oauth/v2/keys
